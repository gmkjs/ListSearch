import { HostblDirective } from './hostbl.directive';

describe('HostblDirective', () => {
  it('should create an instance', () => {
    const directive = new HostblDirective();
    expect(directive).toBeTruthy();
  });
});
